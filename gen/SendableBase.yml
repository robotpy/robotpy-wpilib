---

extra_includes:
- wpi/sendable/SendableBuilder.h

classes:
  SendableBase:
    shared_ptr: true
    ignored_bases:
    - wpi::SendableHelper<SendableBase>
    methods:
      SendableBase:
        ignore: true
