---

extra_includes:
- wpi/sendable/SendableBuilder.h

classes:
  Servo:
    shared_ptr: true
    methods:
      Servo:
      Set:
      SetOffline:
      Get:
      SetAngle:
      GetAngle:
      GetMaxAngle:
      GetMinAngle:
      InitSendable:
